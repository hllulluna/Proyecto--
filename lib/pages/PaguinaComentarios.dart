import 'package:flutter/material.dart';

class PaguinaComentarios extends StatelessWidget {
  const PaguinaComentarios({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Comentarios",
        style: TextStyle(fontSize: 30),
      ),
    );
  }
}
